# ゆらぎ水槽 — 開発メモ

このファイルは、次にこのリポジトリをさわる人（人間でもエージェントでも）に向けた
決めごとと注意点をまとめたもの。仕様そのものは [README.md](README.md) を参照。

---

## 大前提

**`index.html` 1枚で完結させる。** これは制約ではなく設計方針。

- ビルド不要、依存パッケージなし、サーバー処理なし
- 外部リソースは Google Fonts のみ。**画像素材は1枚も使わない**（すべて Canvas で手描き）
- ファイルを分割したくなったら、まず「本当に必要か」を疑う。1ファイルであることが
  「開けば動く」「どこにでも置ける」を支えている

現在およそ 4,500 行。1万行を超えたら分割を再検討してよい。

---

## ファイルの地図

`index.html` は `<style>` → `<body>` → `<script>` の順。スクリプトは1つの IIFE で、
中はコメントバナーで区切ってある。

| セクション | 内容 |
|---|---|
| `catalogue` | `SPECIES` / `DECOR` / `BIOMES` / `HOME` / `UNLOCKS` — **データはすべてここ** |
| `state` | `state` と `tank`、座標の正規化 |
| `helpers` | `waterTop()` `sandTop()` `sandBase()` `toast()` など |
| `save / load` | localStorage の読み書きと起動時の追いつき計算 |
| `entities` | `makePet()` `makeDecor()` とサイズ計算 |
| `economy` | 満腹度・水質・コイン・解放条件の進行 |
| `simulation` | 毎フレームの挙動（`updatePet` ほか） |
| `drawing: creatures` | 生きものの描画 |
| `drawing: decor` | かざりの描画と形状ヘルパ |
| `drawing: scene` | 水・砂・光・泡など背景 |
| `previews` | ショップ・図鑑・カード用の小さい描画 |
| `layout / loop` | `resize()` と `requestAnimationFrame` ループ |
| `hud sync` / `roster` / `interaction` | 画面まわり |
| `boot` | 起動処理 |

**新しい生きものやかざりを足すだけなら、触るのは `catalogue` と `drawing` の2箇所。**

---

## よくある作業

### 生きものを1種類ふやす

1. `SPECIES` に定義を追加（`kind` は `fish` / `shrimp` / `snail` / `eel`）
2. `HOME` に所属を追加 — **忘れるとショップに出ない**
3. 見た目が既存のパラメータ（`ratio` `c1` `c2` `fin` `tail` `mark`）で足りるか検討する。
   足りなければ `mark` を1つ増やす。新しい描画関数を足すのは最後の手段
4. 解放させるなら `UNLOCKS` に条件を追加
5. ショップ・図鑑・カードのプレビューは同じ描画関数を呼ぶので、**追加作業は不要**

### かざりを1つふやす

1. `DECOR` に定義、`HOME` に所属
2. `drawDecor()` の `switch` に `case` を追加
3. 砂に接地する立体なら `SOLID_DECOR` にも登録（接地影がつく）

### 水槽（地域）を1つふやす

1. `BIOMES` に色（`water` / `waterNight` / `sand` / `sandNight` / `pebble` / `mound` / `ray`）と
   価格・`order` を追加
2. その地域の生きものとかざりを `HOME` に割り当てる
3. **コケ取り役を必ず1種入れる。** 水質が下がりっぱなしになる
4. 解放条件をいくつか用意して、育てる目標を作る

---

## 描画のルール

- **座標系** — 生きものは中心が原点、右向きで描く（左向きは `g.scale(-1, 1)`）。
  かざりは砂に接する点が原点、上が負のY
- **`g.save()` と `g.restore()` は必ず対で。** 1つでも漏れると毎フレーム状態スタックが
  伸び続け、他の描画が壊れる（実際にチンアナゴの頭で踏んだ）
- **`mulberry32(seed)` は毎フレーム引き直す。** そのため**乱数を引く回数がフレーム間で
  変わってはいけない。** 時刻や状態で分岐して呼ぶ回数が変わると、模様がちらつく
- 色は必ず明示する。夜モードは `BIOMES[].*Night` を使う
- 生成的な形は `blobPath()`（歪んだ閉曲線）と `limbPath()`（先細る帯）が用意してある。
  石・流木・サンゴ・チンアナゴはこれで描いている

---

## 状態とセーブ

```
state = { coins, nextId, savedAt, current, tanks: { <biome>: tank }, dex, food }
tank  = { biome, name, dirt, night, seed, pets[], decor[], px }
```

- **コインと図鑑は全体で共有。満腹度・水質・夜／昼は水槽ごと。**
- `tank.px` は座標の単位。**表示中の水槽だけがピクセル**で、他は 0〜1 の正規化値。
  切り替えのたびに `normTank()` / `denormTank()` で変換する
- 時間の進行（満腹度・水質・コイン・解放条件）は**表示していない水槽でも進む**
- セーブキーは `yuragi.aquarium.v2`。**形式を変えるときは末尾の版を上げる。**
  互換を保つ場合は `load()` に移行関数を足し、保たない場合は起動時に旧キーを捨てる

---

## 踏んだ落とし穴（同じ轍を踏まないために）

| 症状 | 原因 |
|---|---|
| 石巻貝が永久に空腹 | 貝の更新処理が `tryEat` に到達する前に `return` していた。**`kind` を足したら、餌・当たり判定・プレビューの全経路を通るか確認する** |
| エアストーン2つで片方しか泡が出ない | 泡の上限が**全体共有**で、ループの先頭の個体が空き枠を独占していた。**上限は発生源ごとに配る** |
| チンアナゴがドクロに見える | 白い球に黒い点、は骨。**目を輪郭から張り出させ、体の模様を頭までつなげる**と生きものに見える |
| 描画がだんだん壊れる | `g.save()` の戻し漏れ |
| 小さい部品が読めない | 実寸（画面上7〜8px）で確認していなかった。**必ず game 内のサイズで見る** |

---

## テストのしかた

自動テストはない。ローカルで開いて目で見る。

```
python3 -m http.server 8000
```

`file://` でも動くが、`localStorage` の挙動を本番に合わせたいときは http で見る。

**ブラウザで確認するときの注意:**
- `localStorage` を書き換えてリロードする場合、`beforeunload` の自動セーブが
  上書きしてくる。**書き換えたら `localStorage.setItem = () => {}` で塞いでから遷移する**
- 時間のかかる挙動（解放条件、留守番のコイン）は `savedAt` を過去にした
  セーブを差し込むと一気に検証できる

**リリース前に見るところ:**
1. 新規セーブでの起動（`localStorage.clear()`）
2. 3つの水槽それぞれのショップの中身
3. 図鑑の種類数と分類
4. スマホ幅（375px）でHUDがはみ出さないか
5. コンソールエラーが0件か

---

## 公開

`main` に push すると GitHub Pages が自動で反映する（1〜2分）。

https://yusuke0701.github.io/yuragi-suisou/

---

## 書きかたの決めごと

- **コードに emoji は使わない**
- UI の文言は日本語。ひらがな多めのやわらかい調子で揃える
  （「えさ」「そうじ」「あかり」「にがす」「しまう」）
- 生きものの説明文は1〜2文。事実を1つと、性格を1つ
- コミットは Conventional Commits（`feat:` `fix:` `refactor:` `docs:`）。
  本文には**なぜそうしたか**を書く。何をしたかは差分を見れば分かる
- コメントは「何をしているか」ではなく「**なぜそうなっているか**」を書く
